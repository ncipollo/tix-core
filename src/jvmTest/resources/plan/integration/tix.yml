jira:
    no_epics: false
    url: 'https://tix-test.atlassian.net'
    tickets: 
        default:
            project: TIX
    workflows:
        after_each:
            - label: cleanup
              actions:
                  - type: delete_issuex
                    arguments:
                        issue: $ticket.jira.key